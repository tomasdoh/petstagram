<h1>Petstagram</h1>


<% @posts.each do |post| %>
  <p><%= post.caption %></p>
  <%= image_tag(post.image, size: "240x160") if post.image.attached? %><br>
  <p><%= link_to 'Show', post_path(post) %></p>
<% end %>
